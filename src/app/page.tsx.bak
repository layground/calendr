'use client'

import { useState } from 'react'
import { Logo } from '@/components/ui/Logo'
import { ThemeToggle } from '@/components/ui/ThemeToggle'
import { MonthView } from '@/components/calendar/MonthView'
import { EventDetails } from '@/components/events/EventDetails'
import { BottomSheet } from '@/components/events/BottomSheet'
import { Event } from '@/lib/types/event'

// Temporary mock data until we implement the backend
const mockEvents: Event[] = [
  {
    id: '1',
    title: 'Independence Day',
    description: 'National Independence Day celebration with parades and cultural performances.',
    startDate: new Date(2024, 7, 17, 8, 0),
    endDate: new Date(2024, 7, 17, 22, 0),
    location: {
      name: 'National Monument, Jakarta',
      coordinates: {
        lat: -6.1751,
        lng: 106.8272
      }
    },
    coverImage: 'https://images.unsplash.com/photo-1576353769312-bfefcc20ecdf',
    accessLabels: ['parking', 'wheelchair', 'public-transport'],
    isPublicHoliday: true,
    isOptionalHoliday: false,
    articleUrl: 'https://example.com/independence-day-2024',
    region: 'Jakarta',
    country: 'Indonesia'
  }
]

export default function Home() {

import { Logo } from '@/components/ui/Logo'  return (

import { ThemeToggle } from '@/components/ui/ThemeToggle'    <div className="font-sans grid grid-rows-[20px_1fr_20px] items-center justify-items-center min-h-screen p-8 pb-20 gap-16 sm:p-20">

import { MonthView } from '@/components/calendar/MonthView'      <main className="flex flex-col gap-[32px] row-start-2 items-center sm:items-start">

import { EventDetails } from '@/components/events/EventDetails'        <Image

import { BottomSheet } from '@/components/events/BottomSheet'          className="dark:invert"

import { Event } from '@/lib/types/event'          src="/next.svg"

          alt="Next.js logo"

// Temporary mock data until we implement the backend          width={180}

const mockEvents: Event[] = [          height={38}

  {          priority

    id: '1',        />

    title: 'Independence Day',        <ol className="font-mono list-inside list-decimal text-sm/6 text-center sm:text-left">

    description: 'National Independence Day celebration with parades and cultural performances.',          <li className="mb-2 tracking-[-.01em]">

    startDate: new Date(2024, 7, 17, 8, 0),            Get started by editing{" "}

    endDate: new Date(2024, 7, 17, 22, 0),            <code className="bg-black/[.05] dark:bg-white/[.06] font-mono font-semibold px-1 py-0.5 rounded">

    location: {              src/app/page.tsx

      name: 'National Monument, Jakarta',            </code>

      coordinates: {            .

        lat: -6.1751,          </li>

        lng: 106.8272          <li className="tracking-[-.01em]">

      }            Save and see your changes instantly.

    },          </li>

    coverImage: 'https://images.unsplash.com/photo-1576353769312-bfefcc20ecdf',        </ol>

    accessLabels: ['parking', 'wheelchair', 'public-transport'],

    isPublicHoliday: true,        <div className="flex gap-4 items-center flex-col sm:flex-row">

    isOptionalHoliday: false,          <a

    articleUrl: 'https://example.com/independence-day-2024',            className="rounded-full border border-solid border-transparent transition-colors flex items-center justify-center bg-foreground text-background gap-2 hover:bg-[#383838] dark:hover:bg-[#ccc] font-medium text-sm sm:text-base h-10 sm:h-12 px-4 sm:px-5 sm:w-auto"

    region: 'Jakarta',            href="https://vercel.com/new?utm_source=create-next-app&utm_medium=appdir-template-tw&utm_campaign=create-next-app"

    country: 'Indonesia'            target="_blank"

  }            rel="noopener noreferrer"

]          >

            <Image

export default function Home() {              className="dark:invert"

  const [selectedDate, setSelectedDate] = useState<Date | null>(null)              src="/vercel.svg"

  const [currentDate] = useState(new Date())              alt="Vercel logomark"

  const [selectedEvent, setSelectedEvent] = useState<Event | null>(null)              width={20}

              height={20}

  const handleDateSelect = (date: Date) => {            />

    setSelectedDate(date)            Deploy now

    const events = mockEvents.filter(event => {          </a>

      const eventDate = new Date(event.startDate)          <a

      return (            className="rounded-full border border-solid border-black/[.08] dark:border-white/[.145] transition-colors flex items-center justify-center hover:bg-[#f2f2f2] dark:hover:bg-[#1a1a1a] hover:border-transparent font-medium text-sm sm:text-base h-10 sm:h-12 px-4 sm:px-5 w-full sm:w-auto md:w-[158px]"

        eventDate.getFullYear() === date.getFullYear() &&            href="https://nextjs.org/docs?utm_source=create-next-app&utm_medium=appdir-template-tw&utm_campaign=create-next-app"

        eventDate.getMonth() === date.getMonth() &&            target="_blank"

        eventDate.getDate() === date.getDate()            rel="noopener noreferrer"

      )          >

    })            Read our docs

    if (events.length > 0) {          </a>

      setSelectedEvent(events[0])        </div>

    }      </main>

  }      <footer className="row-start-3 flex gap-[24px] flex-wrap items-center justify-center">

        <a

  return (          className="flex items-center gap-2 hover:underline hover:underline-offset-4"

    <div className="flex min-h-screen-dynamic flex-col md:flex-row">          href="https://nextjs.org/learn?utm_source=create-next-app&utm_medium=appdir-template-tw&utm_campaign=create-next-app"

      {/* Left panel - Calendar */}          target="_blank"

      <div className="flex w-full flex-col gap-6 border-b border-gray-200 p-4 dark:border-gray-700 md:w-[400px] md:border-b-0 md:border-r">          rel="noopener noreferrer"

        <div className="flex items-center justify-between">        >

          <Logo />          <Image

          <ThemeToggle />            aria-hidden

        </div>            src="/file.svg"

        <MonthView            alt="File icon"

          year={currentDate.getFullYear()}            width={16}

          month={currentDate.getMonth()}            height={16}

          events={mockEvents}          />

          onSelectDate={handleDateSelect}          Learn

        />        </a>

      </div>        <a

          className="flex items-center gap-2 hover:underline hover:underline-offset-4"

      {/* Right panel - Event Details (desktop) */}          href="https://vercel.com/templates?framework=next.js&utm_source=create-next-app&utm_medium=appdir-template-tw&utm_campaign=create-next-app"

      <div className="hidden flex-1 md:block">          target="_blank"

        {selectedEvent ? (          rel="noopener noreferrer"

          <EventDetails event={selectedEvent} />        >

        ) : (          <Image

          <div className="flex h-full items-center justify-center">            aria-hidden

            <p className="text-gray-500">Select a date to view events</p>            src="/window.svg"

          </div>            alt="Window icon"

        )}            width={16}

      </div>            height={16}

          />

      {/* Bottom sheet - Event Details (mobile) */}          Examples

      <div className="md:hidden">        </a>

        <BottomSheet        <a

          event={selectedEvent}          className="flex items-center gap-2 hover:underline hover:underline-offset-4"

          onClose={() => setSelectedEvent(null)}          href="https://nextjs.org?utm_source=create-next-app&utm_medium=appdir-template-tw&utm_campaign=create-next-app"

        />          target="_blank"

      </div>          rel="noopener noreferrer"

    </div>        >

  )          <Image

}            aria-hidden
            src="/globe.svg"
            alt="Globe icon"
            width={16}
            height={16}
          />
          Go to nextjs.org →
        </a>
      </footer>
    </div>
  );
}
